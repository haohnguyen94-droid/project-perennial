# Database tooling (schema/migrations track — @bryan).
# Cohen's YouTube pipeline deps live in requirements.txt on cohen-working;
# consolidate the two files when branches merge.
sqlalchemy>=2.0,<3
alembic>=1.13,<2
psycopg[binary]>=3.1,<4
python-dotenv>=1.0,<2
tzdata; sys_platform == "win32"
